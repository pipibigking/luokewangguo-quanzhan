# 🚀 项目启动指南

## ✅ 当前状态

**后端服务**：✅ 正在运行 (http://localhost:8002)
- 图片路径：`/images/` - 已挂载
- 图标路径：`/icons/` - 已挂载
- API路径：`/api/` - 已配置

**前端配置**：✅ 已更新
- Vite代理已配置到8002端口
- 支持 `/api/`、`/images/`、`/icons/` 三个路径的代理

---

## 📋 完整启动步骤

### 1️⃣ 后端服务（已运行）
状态：✅ 正在运行 (端口8002)
- 如果需要重启：
  ```cmd
  cd backend
  python main.py
  ```

### 2️⃣ 启动前端服务
在**新的CMD终端**中执行：
```cmd
cd D:\Trae_project\开发1\洛克王国异色html\yise_Window\frontend
npm install  # 首次运行需要
npm run dev
```

Vite会显示一个地址（通常是 http://localhost:5173 或类似地址），在浏览器中打开即可！

---

## 📝 配置说明

### 后端配置
- 端口：8002
- 数据库：`locke_pets.db`
- 图片目录：`../images`
- 图标目录：`../wiki_style_icons`

### 前端配置 (vite.config.ts)
```typescript
proxy: {
  '/api': { target: 'http://localhost:8002' },
  '/images': { target: 'http://localhost:8002' },
  '/icons': { target: 'http://localhost:8002' }
}
```

---

## 🖼️ 资源路径说明

| 资源 | 访问路径 | 实际位置 |
|------|----------|----------|
| 精灵图片 | `/images/{图片名}.png` | `yise_Window/images` |
| 属性图标 | `/icons/{属性}.svg` | `yise_Window/wiki_style_icons` |
| API数据 | `/api/pets` | 后端提供 |

---

## 📚 功能验证

在浏览器中测试以下地址（如果后端运行）：
- 检查API：http://localhost:8002/api/pets
- 检查图片：http://localhost:8002/images/大耳帽兜.png
- 检查图标：http://localhost:8002/icons/草.svg

---

## 🎉 完成！

现在启动前端，打开浏览器，就可以看到完整的精灵展示界面了！

所有图片和属性图标都会正常显示！ ✨
